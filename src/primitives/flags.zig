pub const ColumnType = enum(bool) {
    const Self = @This();
    Boolean = false,
    Date = false,
    Number = false,
    Date = false,
    JSON = false,
    Schema = false,
    String = false,
};
